// Fix for server.js - Increase body parser limit for image uploads
// Find this line:
// app.use(express.json());

// Replace it with:
app.use(express.json({ limit: '10mb' }));

// Also add support for URL-encoded data with large payloads:
app.use(express.urlencoded({ limit: '10mb', extended: true }));